import AboutHeroSection from "../Components/AboutHeroSection";

const About = () => {
  return (
    <div>
      <div className="bg-white min-h-screen">
        <AboutHeroSection />
        {/* Add additional sections below (mission, team, etc.) */}
      </div>
    </div>
  );
};

export default About;
